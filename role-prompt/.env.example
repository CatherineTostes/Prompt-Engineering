# Copie este ficheiro: cp .env.example .env
# Edite .env e cole a sua chave (https://platform.openai.com/api-keys).
# O ficheiro .env não deve ser commitado — só este exemplo (.env.example) vai para o GitHub.

OPENAI_API_KEY=
